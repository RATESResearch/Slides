[tool.poetry]
name = "slides"
version = "0.1.0"
description = "Sphinx extension to extract slide content embedded in narrative content for generating sphinx-revealjs presentations."
authors = ["Andrew Ernest <anernest@ratesresearch.org>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
